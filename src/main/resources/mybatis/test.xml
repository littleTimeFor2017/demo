<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springdemo.testtrans.ITestMapper">
    <select id="query" resultType="java.lang.Integer">
        select num from `test_order` where id = 1
    </select>

    <insert
            id="saveUser" parameterType="com.example.springdemo.testtrans.OrderEntity"
            useGeneratedKeys="true">
        insert into test_order(price,num) values (#{price},#{num})
    </insert>
</mapper>